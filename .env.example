# CaseProof runs in synthetic local mode without provider credentials.
# Add only public Supabase browser values after a separately authorized setup.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
